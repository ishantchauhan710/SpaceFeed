import { Button } from "@mui/material";
import { styled } from "@mui/system";

// A MUI Button with enhanced text styles
const ActionButton = styled(Button)({
  textTransform: "none",
  fontSize: "14px",
  fontWeight: "500",
});

export default ActionButton;
